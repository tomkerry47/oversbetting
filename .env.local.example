NEXT_PUBLIC_SUPABASE_URL=your-supabase-url-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here
NEXT_PUBLIC_ARENA_EMBED_KEY=your-arena-embed-key-here
RAPIDAPI_KEY=your-rapidapi-key-here
USE_RAPIDAPI=true

# Form/table enrichment is on by default. Odds are separate because they use
# per-match calls and can burn through the monthly RapidAPI quota quickly.
ENRICH_FIXTURES=true
ENRICH_ODDS=false
ROUND_REQUEST_BUDGET=100

# Set to 'true' to use Chromium-based requests (bypasses 403 errors but slower)
USE_BROWSER_API=false

# When RAPIDAPI_KEY is set the app automatically uses the SofaScore RapidAPI
# (sofascore.p.rapidapi.com).  Set to 'false' to force the unofficial SofaScore
# API even when a key is present.
# USE_RAPIDAPI=false
